// TODO: searchSchoolZone(schoolName) — POST /api/search → returns blocks + zone data (UC1)
// TODO: getHiddenGems(schoolID) — GET /api/search/hidden-gems → returns undervalued blocks (UC3)
// TODO: getHeatmapData(schoolID) — GET /api/search/heatmap → returns PSF colour buckets (UC7)
