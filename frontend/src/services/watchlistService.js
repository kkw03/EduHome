// TODO: getWatchlist() — GET /api/watchlist → returns user's watchlist items (UC4)
// TODO: addWatchlistItem(schoolID, minBudget, maxBudget) — POST /api/watchlist
// TODO: removeWatchlistItem(watchID) — DELETE /api/watchlist/<watchID>
