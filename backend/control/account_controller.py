# TODO: AccountController — authentication and session management
# TODO: verifyCredentials(email, pw) — hash-compare password against stored User record
# TODO: createSession(user) — issue session token on successful login
# TODO: destroySession() — invalidate session on logout
