# TODO: SearchController — core search and zone logic
# TODO: validateSchoolName(name) — check name against PrimarySchool dataset
# TODO: searchSchoolPriorityZone(schoolName) — retrieve HDB blocks within 1km Gold / 2km Silver radii (FR1)
# TODO: filterByBudgetAndArea(blocks, maxPrice, minArea) — apply budget & floor area constraints (UC2)
# TODO: applyLeaseDecayGuard(blocks, startYear) — flag blocks with insufficient remaining lease (FR6)
# TODO: Spatial query must execute in < 2 seconds using PostGIS indexing (NFR1)
